[package]
name = "wisp-macros"
description = "#[derive(Script)] for the wisp scripting language"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["derive"] }
quote = "1"
proc-macro2 = "1"
