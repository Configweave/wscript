[package]
name = "wscript-core"
description = "Shared core types for the wscript scripting language: spans, diagnostics, types, bytecode, values"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
