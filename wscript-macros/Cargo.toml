[package]
name = "wscript-macros"
description = "#[derive(Script)] for the wscript scripting language"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["derive"] }
quote = "1"
proc-macro2 = "1"
