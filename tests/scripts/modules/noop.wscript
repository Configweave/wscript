// Imported by `multifile_derive_span_err.wscript`. Lives under
// `modules/` because every `.wscript` directly in `tests/scripts/` is
// run as an entry by the harness, and this file has no `main`.

fn noop() {}
